{
  "mcpServers": {
    "XcodeBuildMCP": {
      "command": "npx",
      "args": ["-y", "xcodebuildmcp@latest"],
      "comment": "Build/test/run via xcodebuild. Requires macOS + Xcode 15+. Scheme: PokeStopBuddyCI."
    },
    "ios-simulator": {
      "command": "npx",
      "args": ["-y", "ios-simulator-mcp@latest"],
      "comment": "Drive the iOS Simulator: AX tree inspection, screenshots, taps. Used for a11y + UI audits."
    }
  }
}
