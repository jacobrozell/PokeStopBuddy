# XcodeGen project definition — single source of truth for targets & schemes.
# Regenerate with: xcodegen generate
name: WaypointWriter
options:
  bundleIdPrefix: com.jacobrozell
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  groupSortPosition: top

settings:
  base:
    SWIFT_VERSION: "5.9"
    DEVELOPMENT_TEAM: 7JT2JB89AV
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    SWIFT_STRICT_CONCURRENCY: complete

targets:
  WaypointWriter:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - App
      - Features
      - Domain
      - Data
      - Persistence
      - DesignSystem
      - Support
      - path: Resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.jacobrozell.waypointwriter
        INFOPLIST_FILE: Resources/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1,2" # iPhone + iPad
        SUPPORTS_MACCATALYST: NO
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
    # No SwiftLint prebuild script: CI lints before xcodebuild and the pre-commit hook
    # covers local dev. Running it as a script phase tangles with Xcode 16's user-script
    # sandbox (deny file-read-data on source dirs) for zero added safety.

  WaypointWriterTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - Tests/Unit
      - Tests/Accessibility
    dependencies:
      - target: WaypointWriter
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.jacobrozell.waypointwriter.tests
        GENERATE_INFOPLIST_FILE: YES

  WaypointWriterUITests:
    type: bundle.ui-testing
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - Tests/UI
    dependencies:
      - target: WaypointWriter
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.jacobrozell.waypointwriter.uitests
        GENERATE_INFOPLIST_FILE: YES

schemes:
  # Fast PR scheme: unit + accessibility only (keeps PRs to minutes).
  WaypointWriterCI:
    build:
      targets:
        WaypointWriter: all
    test:
      targets:
        - WaypointWriterTests
      gatherCoverageData: true
    run:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

  # Full scheme: app + UI tests.
  WaypointWriter:
    build:
      targets:
        WaypointWriter: all
        WaypointWriterTests: [test]
        WaypointWriterUITests: [test]
    test:
      targets:
        - WaypointWriterTests
        - WaypointWriterUITests
    run:
      config: Debug
